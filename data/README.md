# Runtime Data

Generated data stays local and is ignored by Git.

- `raw/`: Wikipedia API responses and source metadata.
- `images/`: downloaded source images.
- `processed/`: OCR, BLIP captions, VLM gate results, annotations, and graph JSON.

Keep source URL, page revision, image URL, caption, and license metadata for every image.
